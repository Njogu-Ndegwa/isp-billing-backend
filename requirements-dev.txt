-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
aiosqlite>=0.20
greenlet>=3.0
# time_machine (not freezegun): it patches the clock at the C level, so the
# direct `datetime.utcnow` reference SQLAlchemy captured in column defaults
# (models.py `created_at`) is frozen too — freezegun misses saved references.
time-machine>=2.16
